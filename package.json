{
  "name": "my-project",
  "version": "0.0.0",
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "preinstall": "npx only-allow pnpm",
    "lint": "biome check .",
    "lint:fix": "biome check . --write",
    "typecheck": "turbo run typecheck",
    "build": "turbo run build",
    "test": "turbo run test",
    "devAll": "pnpm --filter='@cook-me/*' --parallel dev",
    "dev": "pnpm --filter='@cook-me/api' dev"
  },
  "packageManager": "pnpm@10.22.0",
  "pnpm": {
    "overrides": {}
  },
  "devDependencies": {
    "@biomejs/biome": "^2.3.6",
    "@cook-me/tsconfig": "workspace:^",
    "turbo": "^2.6.1",
    "typescript": "^5.9.3"
  },
  "resolutions": {}
}

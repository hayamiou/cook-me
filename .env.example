# Auth bypass (dev only — NEVER set to true in production)
# Décommenter pour tester les routes sans token Keycloak
# DISABLE_AUTH=true

# Keycloak Configuration
# URLs fixes via tunnels Cloudflare nommés (cook-me.club)
KEYCLOAK_ISSUER=https://keycloak.cook-me.club/realms/cook-me
KEYCLOAK_CLIENT_ID=cook-me-api

# MinIO Configuration
MINIO_USER=minio
MINIO_PASSWORD=minioSecret
MINIO_BUCKET=cook-me-bucket
